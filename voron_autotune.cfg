# TMC autotune -- https://github.com/andrewmcgr/klipper_tmc_autotune
#
# Plan, rationale and validation steps: home-network/docs/voron-tmc-autotune.md
# Reviewed against plugin commit b6c7cfa9 (which is also what is installed).
#
# PHASE 1: X, Y, Z, Z1, Z2, Z3, extruder.
# PHASE 2 (2026-09-08): stepper_mmu_gear and stepper_mmu_selector added below,
#   on fitting the LDO-42STH20-1004ASH pair. The stealthchop_threshold lines in
#   mmu/base/mmu_hardware.cfg were commented out in the SAME change, which is the
#   required order: the selector's threshold was load-bearing until an
#   [autotune_tmc] section existed to take over its mode decision.
#
#   NOT done here, deliberately: raising the gear run_current from 0.5 A toward
#   0.7-0.8 A. The plan doc calls for that as a separate load-tested step, not
#   in the same restart as everything else. See "Phase 2: the MMU".
#
# Autotune never writes through SAVE_CONFIG, so these sections are safe in an
# include (unlike [probe_eddy_current] -- see hosts/voron.md on
# _disallow_include_conflicts).
#
# NOTE: voltage is stated explicitly on every section, and it is load-bearing:
# it feeds PWM_OFS, PWM_GRAD, the chopper hysteresis and every velocity
# threshold. Klipper itself has no supply-voltage parameter for a TMC, so this
# file is the ONLY place the rail is written down.
#
# 24 -> 48 on 2026-09-24, on the eight 5160 sections. The extruder keeps 24 --
# it is the EBB's 2209 on the toolhead, still on the 24 V rail.
#
# THE maxpwmrps CAVEAT, now that it is live. Earlier drafts of this file said to
# patch it before trusting 48. Having measured what it actually does, do not
# bother -- it errs in the safe direction:
#
#   maxpwmrps (motor_constants.py:36) calls self.pwmgrad(fclk, steps)
#   POSITIONALLY, so volts never arrives and pwmgrad falls back to 24. Only
#   maxpwmrps is affected; PWM_GRAD and PWM_OFS reach the driver computed at the
#   real voltage (autotune_tmc.py:554-559), and so does the hysteresis (:517).
#
# So the StealthChop ceiling the plugin BELIEVES in barely moves at 48 V, while
# the one the hardware actually has roughly doubles:
#
#   axis           24 V      48 V as computed      48 V in truth
#   X/Y           484 mm/s     499 mm/s              997 mm/s
#   Z (at 8mm/rev) 43.7        44.4                   88.8
#   MMU gear       35.4 (filament)  43.2              77.8
#   MMU selector  233         297                    535
#
# Every tuning_goal in this file reads that number only to place a crossover
# BELOW it, so a low estimate means switching to SpreadCycle earlier than
# strictly necessary. Nothing is driven past its real ceiling; the cost is some
# noise in a band that would have been silent. The crossovers themselves land
# within a couple of mm/s of where they sat at 24 V, so every piece of reasoning
# in the per-section notes below still holds unchanged.
#
# Worth knowing rather than fixing: Z's real StealthChop ceiling is now 88.8
# mm/s, not 43.7. max_z_velocity 45 used to sit at 103% of it and now sits at
# 51%, and the 60 that dropped steps on 2026-09-10 is now 68%. That does NOT
# make 60 safe by itself -- the drop was measured, this is arithmetic -- but it
# is the reason to re-test rather than assume the old ceiling still binds.
#
# NOTE: run_current is now TRUE on every 5160, as of 2026-09-24. sense_resistor
# was corrected from 0.110 to 0.075, which means every driver had been
# delivering 1.47x its configured current -- and autotune had been computing
# PWM_OFS and the chopper hysteresis from the configured number, not the real
# one. Two different splits came out of that:
#
#   X/Y  stay at 1.2 and now actually get 1.2. A real CURRENT REDUCTION from
#        the 1.76 A the coils were seeing, made because the motors ran very hot
#        even at standstill. Note this cuts holding dissipation roughly in half,
#        so anything tuned against the hot behaviour is worth re-checking.
#   Z    left at the measured-equivalent 0.88, i.e. draw is unchanged and only
#        the label is now honest. Reducing it is a separate decision about
#        gantry sag, not a follow-on from X/Y.
#
# Either way the values autotune computes change, because they were previously
# derived from a current the coils never carried. See the header of the X/Y
# section in voron.cfg.
#
# NOTE: run_current is an input to every value autotune computes, and nothing
# recomputes when it changes. Any SET_TMC_CURRENT after startup silently
# invalidates the tuning for that stepper -- which is why the current reduction
# came out of _HOME_X / _HOME_Y.

# A/B: LDO-42STH48-2004MAH, 0.9 deg / 400 steps. The 48mm part, label-confirmed
# -- the 40mm 42STH40 would feed 1.1ohm/2.8mH/0.35Nm instead of 1.4/2.0/0.44.
# No tuning_goal needed: both are in AUTO_PERFORMANCE_MOTORS.
# sgt: lives HERE, not in [tmc5160]. driver_SGT there is overwritten on every
# tune, with no error and no effect -- a silent failure mode.
# sgt tuned empirically 2026-09-04 at homing_speed 80 and the full 0.9 A
# (the _HOME_X/_HOME_Y current reduction was removed for this). X results,
# all measured COLD:
#    1  no trigger at all -- gantry crashed and ground
#   -5  triggers, hard impact
#   -6  triggers, softer
#   -7  same as -6
#   -8  same again
#   -9  too sensitive
#  -10  too sensitive
# So X's cold band is -8..-5, with impact force flat across -6..-8.
#
# -8 was chosen first and FAILED HOT: after a heat-soaked print it triggered
# early. -8 sat on the sensitive edge of the cold band -- one step from the -9
# that failed -- so it had no margin left for a thermal shift. Chamber heat
# changes belt tension and bearing preload, which changes the load signature
# StallGuard reads, so the band measured cold is not the band when soaked.
# -7 is the middle of the cold band and holds both cold and hot: that margin
# on BOTH sides is the point, not the specific number.
#
# Y stays at -8 while X is at -7. The axes deliberately do NOT match, because
# they fail in OPPOSITE directions.
#
# Y was moved to -7 alongside X on 2026-09-05 and reverted the same day: at -7
# the home skipped a few steps before StallGuard fired. That is the
# too-insensitive failure -- the motor was already losing position by the time
# the stall registered. -8 homes cleanly on Y both cold and heat-soaked.
#
# So X's cold band does not transfer to Y, which was always the risk: Y moves
# the whole gantry rather than just the toolhead, so it presents a different
# load signature. X had to move AWAY from the sensitive edge to survive heat;
# Y has to sit ON it to trigger at all. Y's own band has still never been
# swept -- if -8 ever fails hot on Y the way it did on X, sweep Y directly
# (cold, homing_speed 80, full current) rather than borrowing another number
# from X.
# sgt retuned 2026-09-14. Two changes invalidated the old -7/-8: run_current
# 0.9 -> 1.2A (StallGuard load scales with current) and homing_speed 80 -> 100
# (faster reads as LESS load, so the band moves ~4 steps MORE negative).
#
# At 1.2A / 100mm/s: -12 is too hard on both. X homes cleanly at -14.
# Y settled at -15, the least-bad of -16..-13: it remains hit and miss and
# has no reliable integer -- Y carries the whole gantry and its load
# signature is noisier than X's. Expect occasional failed homes until the
# mechanical switch lands.
# Mechanical endstops are planned for both; see docs/voron-tmc-autotune.md.
#
# These MUST track homing_speed in voron.cfg. -10 here with homing_speed 100
# grinds the gantry on the first G28.
[autotune_tmc stepper_x]
motor: ldo-42sth48-2004mah
voltage: 48
sgt: -14

[autotune_tmc stepper_y]
motor: ldo-42sth48-2004mah
voltage: 48
sgt: -15

# Z: LDO-42STH48-2004AC. auto -> silent at 0.59 Nm, which is what we want.
# Database says 1.6ohm where the LDO drawing says 1.4 +/-20%; inside tolerance,
# torque and inductance match exactly, so the entry is fine as-is.
# Z: autoswitch, set 2026-09-10. Was `auto`, which resolves to SILENT here --
# stepper_z is not in AUTO_PERFORMANCE_MOTORS and 0.59 Nm clears the 0.3 Nm bar
# (autotune_tmc.py:237-245) -- i.e. StealthChop at every speed, tpwmthrs=0.
#
# That capped Z far below anything the motor or rail could do. StealthChop runs
# out of PWM amplitude at (255 - pwm_ofs) / (pi * pwm_grad) = (255-16)/(pi*14)
# = 5.43 rev/s. The drive train is rotation_distance 40 over gear_ratio 80:16,
# i.e. 8 mm of Z per motor revolution, so the real ceiling is 43.5 mm/s of Z:
#
#     max_z_velocity 30  =  69% of it   worked for years
#     max_z_velocity 45  = 103%         at the limit
#     max_z_velocity 60  = 138%         DROPPED STEPS 2026-09-10
#
# Note autotune logs this as "max PWM speed 217.36" -- that is in
# rotation_distance units and does NOT have the 5:1 gear ratio applied, which is
# why it looks like plenty of headroom and is not.
#
# Z: PERFORMANCE (SpreadCycle at every speed), set 2026-09-25. Was autoswitch,
# whose whole point was a silent StealthChop standstill -- and on 48 V that stopped
# being true: SpreadCycle holding the gantry is measurably QUIETER here than
# StealthChop (owner, by ear, A/B at runtime). StealthChop at 48 V works with a
# sliver of its PWM range at crawl speeds (pwm_scale_sum ~12 of 255).
#
# It also broke the Cartographer's touch. StealthChop below the ~22 mm/s crossover
# meant every touch approach (1 mm/s) ran in it, and it put full-step torque ripple
# into the Z motion: probe-stream tones at 25/50 Hz (= 1x/2x full step at 1 mm/s;
# they move to 50 Hz at 2 mm/s, and are absent with the head stationary) of about
# +/-2.5 um, i.e. ~+/-40% of the approach VELOCITY. The probe firmware triggers on
# a drop in approach rate (src/stm32/cartograpgher_G431.c, idm_home_task: 20 ms
# boxcar of 2 ms deltas, absolute threshold), so those dips stopped the nozzle in
# mid-air at random heights -- 0.05-0.28 mm off the bed, confirmed by the stopped
# frequency lying on the free-air curve at -134 Hz/um. SpreadCycle cut the 25/50 Hz
# tones 65-80%, and CARTOGRAPHER_TOUCH_ACCURACY went from failing every run to
# 10/10 samples, 6.25 um range, 2.0 um stdev (same model, coil 31 C).
#
# Ruled out on the way, so nobody re-chases them: probe noise/fans (1-LSB flicker
# at the 43.5 Hz resolution), Pi latency, Z backlash (5 um), Z repeatability
# without contact (0.4 um), Cartographer clock sync (7 ppm), and StealthChop
# tuning (pwm_autograd was already on; pwm_grad 7 vs 15 changed nothing).
#
# Blanking time is autotune's default (tbl 1) again. tbl 0 (16 clocks, the chip
# minimum) was tried 2026-09-26 from an accelerometer search
# (home-network/tools/tmc-ripple-tune: LIS2DW vibration during Z moves at
# 1/2/3/5/8 mm/s, 56 trials). It scored 85% of tbl 1's full-step vibration
# (2=128%, 3=158%), with extra_hysteresis 2 to keep hend at 9. It was reverted the
# same day because the front-left motor hummed/jittered while HOLDING, worse or
# better depending on the microstep it stopped at: the classic too-short-blank
# symptom at 48 V, where switching ringing trips the current comparator. The
# ripple it cut no longer matters for touch: the patched Cartographer firmware
# (6.2.0-tj2+) rejects ripple dips with [cartographer touch] hold_distance.
[autotune_tmc stepper_z]
motor: ldo-42sth48-2004ac
voltage: 48
tuning_goal: performance

[autotune_tmc stepper_z1]
motor: ldo-42sth48-2004ac
voltage: 48
tuning_goal: performance

[autotune_tmc stepper_z2]
motor: ldo-42sth48-2004ac
voltage: 48
tuning_goal: performance

[autotune_tmc stepper_z3]
motor: ldo-42sth48-2004ac
voltage: 48
tuning_goal: performance

# Extruder: LDO-36STH20-1004AHG. auto -> PERFORMANCE, not silent: the README
# advice of "silent for the extruder" carries an "unless the motor is very
# small" caveat, and 0.10 Nm is very small. So the toolhead runs SpreadCycle and
# will be no quieter than before. Correct for high flow, not a misconfiguration.
# Set tuning_goal: silent here if quiet ever matters more than consistency.
# voltage stays 24 here and ONLY here: this is the EBB's TMC2209 on the
# toolhead, which is fed from the 24 V rail. The 48 V conversion is the six
# Octopus 5160s and the two MMB EZ5160s, not the machine.
[autotune_tmc extruder]
motor: ldo-36sth20-1004ahg
voltage: 24

# MMU gear + selector: LDO-42STH20-1004ASH, fitted 2026-09-08, replacing a Moons
# MS17HD5P4100 (gear) and a FYSETC 42HS20401B-20-0.5R (selector). 7.2 ohm /
# 7.0 mH / 0.18 Nm / 1.0 A / 200 steps, which is what the database carries.
#
# tuning_goal is explicit on both rather than left to auto (auto lands on
# performance at 0.18 Nm, but via a torque cutoff that says nothing about these
# axes). autoswitch rather than performance, changed 2026-09-08 after performance
# made the SELECTOR hiss at standstill: it had carried stealthchop_threshold: 100
# for years, so it was StealthChop below 100 mm/s including at rest, and
# performance writes tpwmthrs=0xFFFFF + en_spreadcycle, i.e. SpreadCycle even
# while parked and energised at hold current. autoswitch restores a quiet
# standstill without giving up SpreadCycle where the torque is actually needed.
#
# The computed crossovers, against Happy Hare's own speeds:
#
#   gear      StealthChop below  7.1 mm/s filament  (ceiling 35 mm/s)
#             synced printing  ~1.6 mm/s -> StealthChop, and that is the axis's
#             whole duty cycle for hours at a time
#             homing 50 / spool+unload 80 / buffer 150 -> all SpreadCycle
#   selector  StealthChop below 47 mm/s   (ceiling 233 mm/s)
#             standstill -> StealthChop; homing 60 and moves 120 -> SpreadCycle
#
# So every bowden move and every gate move keeps SpreadCycle, and the 0.5 -> 0.7 A
# gear increase is untouched where it was aimed. What changes is the quiet: the
# gear during synced printing, and both motors at rest.
#
# autoswitch is marked EXPERIMENTAL in autotune_tmc.py:86. It is also the only
# goal whose behaviour depends on TPWMTHRS, which maxpwmrps computes from a
# hardcoded 24 V. The rail is 48 now, so that IS in play -- and it lands
# conservative: the crossover moves from 17.7 to 21.6 mm/s of filament rather
# than to the 38.9 the real ceiling would allow. See the header.
#
# run_current stays where it is in mmu_hardware.cfg (gear 0.7 A, selector 0.8 A)
# -- and unlike the Octopus steppers it did NOT move on 2026-09-24, because the
# EZ2209 it replaced really did have the 0.110 sense resistor the config claimed.
# Commanded current equalled coil current before the swap and still does.
# Both old and new motors are 1.0 A rated, so those values survived the swap
# unchanged -- and autotune recomputes nothing if they are altered later, so
# raising the gear current is its own deliberate step.
# GEAR: autoswitch. The plan doc says performance, and its REASONING holds --
# every bowden move the axis makes (homing 50, spool/unload 80, buffer 150 mm/s
# filament) is above its ~35 mm/s StealthChop ceiling, so those moves must be
# SpreadCycle. autoswitch keeps them there: it sets tpwmthrs at ~7 mm/s filament,
# so everything above that is SpreadCycle exactly as performance would be. What
# it adds is a quiet standstill, which performance cannot give because it writes
# SpreadCycle even while parked and energised.
#
# Chosen 2026-09-08, AFTER a broken solder joint on one gear phase was found and
# repaired -- so unlike the first attempt at autoswitch that evening, this is a
# judgement about noise from a motor with all four wires connected. (The fault
# showed as ola=1 OpenLoad_A plus a pwm_grad_auto of 39 against the selector's
# 20; a buzz was not enough motion to expose it, it took a 100 mm move.)
#
# The one thing to watch: synced printing sits at ~1.6 mm/s filament, BELOW the
# crossover, so the spool-dragging duty the axis was sized for now runs in
# StealthChop. At 0.3 rev/s that is essentially DC and well inside what
# StealthChop regulates -- but if bowden loads or synced feeding ever falter,
# performance is one word away and is the documented fallback.
#
# autoswitch is marked EXPERIMENTAL in autotune_tmc.py:86, and is the only goal
# whose behaviour depends on TPWMTHRS -- which maxpwmrps computes from a
# hardcoded 24 V. Live at 48 and conservative; see the header.
[autotune_tmc mmu_stepper unit0_gear]
motor: ldo-42sth20-1004ash
voltage: 48
tuning_goal: autoswitch

# SELECTOR: silent, 2026-09-08. StealthChop everywhere, including its moves.
# Safe here and NOT safe on the gear, for one reason -- headroom:
#
#   selector  StealthChop ceiling 233 mm/s vs selector_move_speed 120,
#             selector_homing_speed 60   -> both comfortably under. Fine.
#   gear      StealthChop ceiling  35 mm/s filament vs gear_from_buffer 150,
#             spool/unload 80, homing 50 -> all ABOVE it. Not fine.
#
# So the selector can run StealthChop end to end; the gear cannot, and is on
# performance above. Kept at silent through the 2026-09-08 gear cable fault,
# because the standstill hiss it fixes was the selector's own and unrelated:
# performance writes SpreadCycle even while parked, which is what stealthchop_
# threshold: 100 used to prevent.
[autotune_tmc mmu_stepper unit0_selector]
motor: ldo-42sth20-1004ash
voltage: 48
tuning_goal: silent
